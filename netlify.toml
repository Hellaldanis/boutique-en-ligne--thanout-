# Netlify Configuration
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  VITE_API_URL = "https://boutique-en-ligne-thanout.onrender.com/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
